-- Enhacing `Mardown` files
--
return {
    --- Preview Markdown files from within Neovim in a floating window
    { "ellisonleao/glow.nvim",        config = true, cmd = "Glow" },
    { 'jghauser/follow-md-links.nvim' }
}
